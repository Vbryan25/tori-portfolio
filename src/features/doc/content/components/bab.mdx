---
title: "Bab"
brand: "Integrity Console"
description: "The proctoring console's component library: 40 components on Base UI and Tailwind v4, with a token layer lifted from the clickthrough prototype rather than redrawn."
status: "In progress"
componentCount: 40
createdAt: "2026-07-21"
updatedAt: "2026-08-21"
new: true
---

Bab is the component library behind the Integrity Console, a proctoring
dashboard for live chat triage, student context and case review. It started as a
13-screen clickthrough in vanilla HTML; this is that prototype's decisions
turned into something an engineer can build from.

The case study is [Integrity Console Design System](/work/bab-design-system).
Everything below is the reference.

<LinkButton href="/storybook/bab/index.html">Browse the Storybook</LinkButton>

## Foundations

Base UI primitives, Tailwind v4, shadcn conventions, Geist Variable. The
dependency list is deliberately dull: the decisions live in the tokens.

### Color

Tokens come across from the prototype's `tokens.css` under the same names, so a
value never means one thing in the clickthrough and another in the React build.
Everything is exposed through Tailwind's `@theme` layer; a component names a
token, never a hex.

```css title="src/index.css"
:root {
  /* Brand: one red for primary actions */
  --primary: #c70036;
  --primary-foreground: #ffffff;

  /* Surfaces: cards float on a warm ground; depth is shadow, not rules */
  --background: #efeceb;
  --bg-deep: #eae7e6;
  --card: #ffffff;
  --surface-nested: #f8f7f6;
  --foreground: #0c0a09;
  --muted-foreground: #7c6d67;

  /* Selection and chrome */
  --sel: #dbd3d1;
  --sel-hover: #e4dedc;
  --pill: #f3f1f1;
  --border: #e8e4e3;
  --border-soft: rgba(124, 109, 103, 0.4);
  --border-rail: rgba(124, 109, 103, 0.18);

  /* The second red. Inbox Close and the live-attempt badge only. */
  --red-deep: #b20030;
}
```

#### Role pairs

Student, instructor and admin each own a background/foreground pair. The chart
series reuse the same hues, so a colour carries one meaning across the whole
product rather than one meaning per view.

| Role       | Background | Foreground |
| ---------- | ---------- | ---------- |
| Student    | `#fae0d1`  | `#b94513`  |
| Instructor | `#dbeafe`  | `#1d4ed8`  |
| Admin      | `#feeeaf`  | `#d97706`  |

#### Status

| Token           | Value     | Use                                     |
| --------------- | --------- | --------------------------------------- |
| `--success`     | `#047857` | Resolved, healthy, within threshold     |
| `--warning`     | `#b45309` | Approaching a threshold                 |
| `--destructive` | `#e7000b` | Failed check, above threshold           |
| `--readonly`    | `#778ba6` | Fields the agent can see but not change |

#### Inbox row states

The one place the palette gets fussy, because three states have to be
distinguishable at a glance down a long list. Unread reads _lighter_ than the
ground; read reads a step darker; selected gets its own warm grey rather than
borrowing `--sel`.

| State    | Rest      | Hover     |
| -------- | --------- | --------- |
| Unread   | `#fdfcfc` | `#f4f0ef` |
| Read     | `#e6e1e0` | `#dcd6d4` |
| Selected | `#d2c8c6` | `#c7bab7` |

### Type

Geist Variable throughout: `--font-heading` is an alias of `--font-sans`, not a
second family. Headings differentiate by weight and size against Tailwind's
default scale; there is no bespoke scale to memorise.

### Radius

One base, everything else derived. Changing `--radius` reshapes the product.

```css
--radius: 0.75rem; /* lg */
--radius-sm: calc(var(--radius) * 0.6);
--radius-md: calc(var(--radius) * 0.8);
--radius-xl: calc(var(--radius) * 1.4);
--radius-2xl: calc(var(--radius) * 1.8);
```

### Elevation

```css
--shadow-card: 0 0 4px rgba(0, 0, 0, 0.15);
```

That is the entire elevation system. The content card is the only shadowed
surface on a page; anything nested inside it uses `--surface-nested` with a
hairline border. A second shadow is a bug.

## Components

40 components in four groups, 24 of them with stories of their own, plus 12
page compositions that assemble the lot at real scale.

| Group        | Count | Contents                                                                                                                                                                                             |
| ------------ | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ui/`        | 21    | Accordion, avatar, badge, button, card, chart, command, dialog, dropdown menu, input, input group, popover, segmented control, skeleton, sonner, switch, table, tabs, textarea, toggle, toggle group |
| `inbox/`     | 10    | Conversation list, list item, message thread, composer, chat header, side panel, role avatar, role badge, command palette, close-conversation dialogs                                                |
| `dashboard/` | 5     | Stat tile, trend chart, source card, compare toggle, report metric layout                                                                                                                            |
| `chrome/`    | 4     | App shell, icon rail, secondary nav, topbar                                                                                                                                                          |

### ui/

<StoryEmbed
  id="ui-button--all-variants"
  title="UI / Button: all variants"
  width="760"
  height="80"
  caption="Primary carries the one red that means ‘primary action’. Destructive is a different token again: the second red, --red-deep, is reserved for the inbox Close and the live-attempt badge and appears in neither of these."
/>

<StoryEmbed
  id="ui-badge--all-variants"
  title="UI / Badge: all variants"
  width="760"
  height="72"
  caption="Where the role pairs show up. The same three hues carry the series on the Overview chart, so a colour means one thing across the product."
/>

<StoryEmbed
  id="ui-table--default"
  title="UI / Table"
  width="900"
  height="200"
  caption="Rows on the card surface, hairline rules rather than a second shadow."
/>

<StoryEmbed
  id="ui-segmentedcontrol--default"
  title="UI / SegmentedControl"
  width="760"
  height="80"
/>

<StoryEmbed
  id="ui-accordion--default"
  title="UI / Accordion"
  width="900"
  height="160"
  caption="The pattern the Inbox context panel is built from."
/>

### dashboard/

<StoryEmbed
  id="dashboard-stattile--grid"
  title="Dashboard / StatTile: grid"
  width="900"
  height="180"
  caption="One elevation in practice: the tiles sit on --surface-nested inside the shadowed card, separated by a hairline rather than a shadow of their own."
/>

<StoryEmbed
  id="dashboard-trendchart--multi-series"
  title="Dashboard / TrendChart: multi-series"
  width="1160"
  height="260"
  caption="The lead series is near-black rather than red, which keeps red free to mean ‘above threshold’ wherever it turns up in a chart."
/>

<StoryEmbed
  id="dashboard-sourcecard--cards"
  title="Dashboard / SourceCard"
  width="900"
  height="320"
/>

### chrome/

<StoryEmbed
  id="chrome-secondarynav--default"
  title="Chrome / SecondaryNav"
  width="900"
  height="370"
  caption="Selected destinations take the filled icon; actions never fill."
/>

### Page compositions

Inbox · Knowledge Sources · Knowledge Content · Settings Integrations · Reports
Overview · Reports All · Conversations by Role · Exam Completion · Lockdown
Browser · Response Time · Room Scan · Screen Share

## Changelog

- **2026-08-21**: 40 components and 12 page compositions published to Storybook.
- **2026-08-20**: Component contract defined.
- **2026-07-21**: Token tiers drafted.
